<h1>Lista de productos</h1>
<ul>
  {{#each products}}
    <li>
      <div>
        <h2>{{this.title}}</h2>
        <p>${{this.price}}</p>
        <img src={{this.thumbnails}} alt={{item.title}} width="100" height="100">
      </div>
    </li>
  {{/each}}
</ul>